server:
  port: 8081

spring:
  application:
    name: lightning

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${ip}:3306/lightning?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123root

# 配置mybatis实体类和xml映射
mybatis:
  mapper-locations: classpath:mappers/*xml
  type-aliases-package: com.example.springboot.entity
# ip
ip: localhost